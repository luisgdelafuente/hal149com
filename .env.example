# Sanity CMS Configuration
# Create a project at https://sanity.io/manage and get these values

# Required: Your Sanity project ID (found in sanity.io/manage)
PUBLIC_SANITY_PROJECT_ID=your-project-id

# Required: Dataset name (default is 'production')
PUBLIC_SANITY_DATASET=production

# Optional: API token for write operations and preview mode
# Generate at: https://sanity.io/manage -> Your Project -> API -> Tokens
# Permissions needed: Editor (for write) or Viewer (for preview only)
SANITY_API_TOKEN=your-api-token

# Netlify deployment (these are set automatically by Netlify)
# CONTEXT=production
# URL=https://hal149.com
